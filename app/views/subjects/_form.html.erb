<%= form_for(@subject) do |f| %>
  <% if @subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subject.errors.count, "error") %> prohibited this subject from being saved:</h2>

      <ul>
      <% @subject.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :term_id %>
    <%= f.collection_select(:term_id, Term.find(:all), :id, :title, include_blank: '学期を選択') %>
  <div class="field">
    <%= f.label :page_title %><br />
    <%= f.text_field :page_title %>
  </div>
  <div class="field">
    <%= f.label :screen_title %><br />
    <%= f.text_field :screen_title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
