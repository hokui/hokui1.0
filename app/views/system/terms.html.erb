<h1>学期管理</h1>
<% if @notice.blank? %>
<section>
  <table class="admin-index">
    <tr>
      <th class="id"></th>
      <th class="sub">Title</th>
      <th class="bbs_group">BBS-Group</th>
    </tr>

  <% @bbs_groups.each do |bbs_group| %>
    <tr>
      <td><%= bbs_group.id %></td>
      <td><%= bbs_group.title %></td>
      <td><%= bbs_group.bbs_group_title %></td>
    </tr>
  <% end %>
  </table>
</section>
<% else %>
  <%= @notice %>
<% end %>
<%= link_to 'new', action: 'new_term' %>
