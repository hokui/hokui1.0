<%= form_for(@bbs_topic) do |f| %>
  <% if @bbs_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bbs_topic.errors.count, "error") %> prohibited this bbs_topic from being saved:</h2>

      <ul>
      <% @bbs_topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bbs_group_id %><br />
    <%= f.number_field :bbs_group_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
