<h1>Listing bbs_topics</h1>

<table>
  <tr>
    <th>Bbs group</th>
    <th>User</th>
    <th>Title</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bbs_topics.each do |bbs_topic| %>
  <tr>
    <td><%= bbs_topic.bbs_group_id %></td>
    <td><%= bbs_topic.user_id %></td>
    <td><%= bbs_topic.title %></td>
    <td><%= link_to 'Show', bbs_topic %></td>
    <td><%= link_to 'Edit', edit_bbs_topic_path(bbs_topic) %></td>
    <td><%= link_to 'Destroy', bbs_topic, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bbs topic', new_bbs_topic_path %>
